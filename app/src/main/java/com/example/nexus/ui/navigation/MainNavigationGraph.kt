package com.example.nexus.ui.navigation

/**
 * Object responsible for storing main navigation routes within the app.
 */
object MainNavigationGraph {
    const val DASHBOARD = "DashboardScreen"  // Rota para o dashboard principal
    const val ORDERS = "orders"  // Rota para a tela de ordens
    const val SCORE = "score"  // Rota para a tela de pontuação
    const val WORK_REGISTRATION = "work_registration"  // Rota para a tela de registro de trabalho


    const val TEST1 = "test1"  // Rota para a tela de registro de trabalho
    const val TEST2 = "test2"  // Rota para a tela de registro de trabalho
    const val TEST3 = "test3"  // Rota para a tela de registro de trabalho
}